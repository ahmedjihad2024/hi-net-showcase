<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#4CAF50" /> <!-- Default green, can be changed via setInt(id,
    "setBackgroundColor", color) -->
    <corners android:radius="100dp" /> <!-- Circular/Pill radius -->
    <padding
        android:left="8dp"
        android:top="2dp"
        android:right="8dp"
        android:bottom="2dp" />
</shape>